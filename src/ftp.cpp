#include <Arduino.h>
#include "ftp.h"

/*void FTPSend(File file){
    OpenConnection();
    String list[LIST_SIZE];
    ChangeWorkDir(WORK_DIR_PATH);
    ContentList("", list);
    InitFile("Type A");

    CloseConnection();
}*/